import { ArrayTextFilterPipe } from './array-text-filter.pipe';

describe('ArrayTextFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayTextFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
